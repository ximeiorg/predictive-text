# 重要决策记录

## 2026-05-04

### 数据编码: 最长匹配优先
**决策**: 从逐字编码改为最长匹配优先编码。
**原因**: 多字 token（如"我们"）应作为整体被模型预测, 对输入法联想有意义。
**影响**: 930 个多字槽位真正参与训练, 训练 token 数从 3.06B 降至 2.6B.

### 跳过非词表字符（零 UNK）
**决策**: 不在词表中的字符直接跳过, 不产生 [UNK]。
**原因**: UNK 是噪声, 0.56% 的 UNK 率导致模型无法收敛。
**影响**: 训练数据零 UNK, 模型不再被噪声干扰。

### 过滤纯标点多字 token
**决策**: 多字组合中纯标点的（如"——"）不进入词表。
**原因**: 对标点组合对输入法联想无意义, 浪费词表槽位。

### GPU torch 管理
**决策**: 手动安装 GPU wheel + 设置 UV_NO_SYNC=1 环境变量。
**原因**: uv 每次 sync 会将 GPU torch 覆盖为 CPU 版本。
**影响**: uv run 跳过自动 sync, 避免依赖覆盖。
