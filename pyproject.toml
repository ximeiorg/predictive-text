[project]
name = "predictive-text"
version = "0.1.0"
description = "Lightweight Transformer for Chinese predictive text input"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
    "tqdm>=4.65.0",
    "numpy>=1.24.0",
    "wikiextractor>=3.0.0",
    "opencc>=1.0.0",
    "pandas>=2.0.0",
    "pyarrow>=14.0.0",
    "tokenizers>=0.15.0",
    "tensorboard>=2.20.0",
]

[project.optional-dependencies]
mobile = [
    "onnx>=1.15.0",
    "onnxruntime>=1.16.0",
]

[project.scripts]
train = "src.train:main"
build-vocab = "src.build_vocab:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
